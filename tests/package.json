{
  "name": "howard-atlas-tests",
  "private": true,
  "type": "module",
  "description": "Playwright checks for the atlas (/) and the classic page (/classic): stub tiles, stubbed county record, real UI. Run from the repo root with `npm test` after `cd tests && npm install && npx playwright install chromium`.",
  "scripts": {
    "test": "node run.mjs"
  },
  "devDependencies": {
    "leaflet": "^1.9.4",
    "playwright": "^1.49.1"
  }
}
